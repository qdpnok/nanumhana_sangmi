<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nanumhana_login_mypage_v0">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nanumhana_login_mypage_v0">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginPageActivity"/>
        <activity android:name=".MembertypeActivity"/>
        <activity android:name=".PCSignupSubActivity"/>
        <activity android:name=".PCSignupActivity"/>
        <activity android:name=".CASignupSubActivity"/>
        <activity android:name=".CASignupActivity"/>
    </application>


</manifest>